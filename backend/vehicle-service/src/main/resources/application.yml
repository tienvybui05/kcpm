spring:
  application:
    name: vehicle-service
  datasource:
    url: jdbc:mysql://${DB_HOST:mysql_vehicle}:${DB_PORT:3306}/${DB_NAME:vehicle_service}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USER:root}
    password: ${DB_PASS:root}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8088